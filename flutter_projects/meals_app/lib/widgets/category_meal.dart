import 'package:flutter/material.dart';

class CategoryMeal extends StatelessWidget {
  const CategoryMeal({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      
    );
  }
}
